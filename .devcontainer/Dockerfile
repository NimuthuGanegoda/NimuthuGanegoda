# Optional Dockerfile for further customization
FROM mcr.microsoft.com/devcontainers/typescript-node:1-20

# Install additional global tools here if needed
# RUN npm install -g vercel

USER node
WORKDIR /workspace
